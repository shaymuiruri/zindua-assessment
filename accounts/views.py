from django.shortcuts import render

# Create your views here.
# Authentication is handled entirely by djangorestframework-simplejwt.
# The login and refresh endpoints are wired directly in core/urls.py —
# no custom views are needed here for this demo.